# Bug 修复：仪表盘运行时异常

**Date:** 2026-06-21 16:13 CST  
**Duration:** ~20 min

---

## Bug 1: `publish_time` 脏数据导致 DateParseError

### 根因
数据库 `publish_time` 字段存在 1,140 条非日期数据（"喜欢聊天"、"简历处理快"、"今日回复10+次" 等），加上 315 条空字符串。`pd.to_datetime()` 无安全防护时崩溃。

### 修复
`src/ui/trends.py` 第 17-21 行：
```python
# Before (crash)
real["week"] = pd.to_datetime(real["publish_time"], format="mixed").dt.to_period(...)

# After (safe)
real["publish_dt"] = pd.to_datetime(real["publish_time"], format="mixed", errors="coerce")
real = real[real["publish_dt"].notna()]
if len(real) < 10:
    st.info("有效发布日期不足 10 条，无法计算趋势。")
    return
real["week"] = real["publish_dt"].dt.to_period("W").astype(str)
```

---

## Bug 2: `SkillROIModel()` 缺少必需参数

### 根因
`SkillROIModel.__init__` 要求 `jobs: pd.DataFrame` 参数，但 UI 层用 `SkillROIModel()` 无参构造。

### 修复
`src/ui/trends.py` 第 49 行：
```python
# Before
roi_model = SkillROIModel()
roi_results = roi_model.compute_roi(jobs)   # 方法名也错了

# After
roi_model = SkillROIModel(real)
roi_results = roi_model.analyze()           # 正确方法名
```

---

## Bug 3: ROI 排行表 `r["rank"]` KeyError

### 根因
`analyze()` 返回结果无 `rank` 字段。

### 修复
`src/ui/trends.py` 第 56-61 行：
```python
# Before
for r in roi_results[:20]:
    table_data.append({"排名": r["rank"], ...})

# After
for idx, r in enumerate(roi_results[:20], 1):
    table_data.append({"排名": idx, ...})
```

---

## Bug 4: 城市对比页 `"城市"` 列不存在

### 根因
`city_comparison_adjusted()` 输出列名为 `city`（英文），但 UI 期望 `城市`（中文）。

### 修复
`src/ui/cities.py` 第 27 行：
```python
df_cmp = city_comparison_adjusted(real)
if "city" in df_cmp.columns:
    df_cmp = df_cmp.rename(columns={"city": "城市"})
```

---

## 验证
- 0 未捕获异常 / 40/40 测试通过
- http://localhost:8501 正常响应
